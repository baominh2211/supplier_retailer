# Database - Local
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/b2b_marketplace

# Database - Render (example)
# DATABASE_URL=postgresql+asyncpg://user:password@host/database

# JWT
SECRET_KEY=your-super-secret-key-change-this-in-production-min-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS - comma separated
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-app.vercel.app

# ========== EMAIL SETTINGS ==========
# Option 1: Resend API (Recommended - works on Render)
# Get API key from https://resend.com (free 3000 emails/month)
RESEND_API_KEY=re_xxxxxxxxx
EMAIL_FROM=B2B Marketplace <onboarding@resend.dev>

# Option 2: Gmail SMTP (may not work on some hosts)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173

# ========== AI SETTINGS (ChatGPT) ==========
# Get API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-3.5-turbo

# Debug
DEBUG=false
